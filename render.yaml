services:
  - type: web
    name: medsim
    runtime: python
    rootDir: clinical-sim-backend-bundle-fresh-20260226-222555/clinical-sim-backend
    buildCommand: pip install -e .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
